## SELECT
```
SELECT first_name, last_name
FROM customers;
```

## SELECT ALL
- SELECT all columns using *

```
SELECT * 
FROM Customers;
```

## SELECT FROM WHERE Clause
```
SELECT *
FROM Customers
WHERE last_name = "Doe";
```

